#==========================================================================================================
 
SET(features_LIB
    lfeatures.cpp
    lgeometry.cpp)


ADD_LIBRARY(lfeatures SHARED ${features_LIB})
